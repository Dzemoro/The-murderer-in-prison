using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sResultVariables : MonoBehaviour
{

    public bool _open;

    // Start is called before the first frame update
    void Start()
    {
        _open = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
